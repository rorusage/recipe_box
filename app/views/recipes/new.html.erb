<div class="col-md-12">
   <h1 class="text-center">Add new recipe</h1>
   <%= simple_form_for @recipe do |f|%>
    <div class="form-group">
      <div id="photos">
        <%= f.simple_fields_for :photos do |photo| %>
          <%= render 'photo_fields', :f => photo %>
        <% end %>
        <div class="links">
          <%= link_to_add_association 'Add_Photo', f, :photos, class: "btn btn-info" %>
        </div>
      </div>

      <%= f.input :title, input_html: {class: "form-control"} %>
      <%= f.input :content, input_html: {class: "form-control"} %>

      <h3>Ingredients</h3>
      <div id="ingredients">
        <%= f.simple_fields_for :ingredients do |ingredient| %>
          <%= render 'ingredient_fields', :f => ingredient %>
        <% end %>
        <div class="links">
          <%= link_to_add_association 'Add_Ingredient', f, :ingredients, class: "btn btn-info" %>
        </div>
      </div>

      <h3>Steps</h3>
      <div id="steps">
        <%= f.simple_fields_for :steps do |step| %>
          <%= render 'step_fields', :f => step %>
        <% end %>
        <div class="links">
          <%= link_to_add_association 'Add Step', f, :steps, class: "btn btn-info" %>
        </div>
      </div>
    </div>
    <%= f.submit "Submit", class: "btn btn-success btn-block ", data: {disable_with: "Submitting..."}%>
  <% end %>
</div>


<!-- <div class="col-md-12">
  <%= simple_form_for @recipe do |f| %>
    <div class="form-group">
      <%= f.input :title, input_html: {class: "form-control"} %>
      <%= f.input :content, input_html: {class: "form-control"} %>
    </div>
    %h3 Ingredients
    #ingredients
      = f.simple_fields_for :ingredients do |ingredient|
        = render 'ingredient_fields', f: ingredient
      .links
        = link_to_add_association 'add ingredient', f, :ingredients
    = f.submit
  <% end %>
</div>
 -->

<!-- <div class="col-md-12">
   <%= simple_form_for @recipe do |f|%>
    <div class="form-group">
      <%= f.input :title, input_html: {class: "form-control"} %>
      <%= f.input :content, input_html: {class: "form-control"} %>
        <%= f.simple_fields_for :ingredients do |ingredient| %>
          <%= ingredient.input :name, input_html: {class: "form-control"} %>
          <%= ingredient.input :amount, input_html: {class: "form-control"} %>
        <% end %>
        <%= f.simple_fields_for :steps do |step| %>
          <%= step.input :step_order, input_html: {class: "form-control"} %>
          <%= step.input :content, input_html: {class: "form-control"} %>
        <% end %>
    </div>
    <%= f.submit "Submit", class: "btn btn-primary", data: {disable_with: "Submitting..."}%>
  <% end %>
</div> -->
