<%= simple_form_for recipe do |f|%>
    <div class="form-group">
      <h3>Photos</h3>
      <div id="photos">
        <%= f.simple_fields_for :photos do |photo| %>
          <%= render 'photo_fields', :f => photo %>
        <% end %>
        <div class="links">
          <%= link_to_add_association 'Add_Photo', f, :photos, class: "btn btn-info" %>
        </div>
      </div>

      <%= f.input :title, input_html: {class: "form-control"} %>
      <%= f.input :content, input_html: {class: "form-control"} %>

      <h3>Ingredients</h3>
      <div id="ingredients">
        <%= f.simple_fields_for :ingredients do |ingredient| %>
          <%= render 'ingredient_fields', :f => ingredient %>
        <% end %>
        <div class="links">
          <%= link_to_add_association 'Add_Ingredient', f, :ingredients, class: "btn btn-info" %>
        </div>
      </div>

      <h3>Steps</h3>
      <div id="steps">
        <%= f.simple_fields_for :steps do |step| %>
          <%= render 'step_fields', :f => step %>
        <% end %>
        <div class="links">
          <%= link_to_add_association 'Add Step', f, :steps, class: "btn btn-info" %>
        </div>
      </div>
    </div>
    <%= f.submit "Submit", class: "btn btn-success btn-block ", data: {disable_with: "Submitting..."}%>
<% end %>
